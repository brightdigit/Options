name: Options
settings:
  LINT_MODE: ${LINT_MODE}
packages:
  StealthyStash:
    path: .
aggregateTargets:
  Lint:
    buildScripts:
      - path: Scripts/lint.sh
        name: Lint
        basedOnDependencyAnalysis: false
    schemes: {}